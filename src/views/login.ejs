<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Login Page</title>
		<!-- Include Bootstrap CSS file -->
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
			rel="stylesheet"
		/>
		<style>
			/* Custom styles */
			body {
				font-family: Arial, sans-serif;
				background-color: #e9e3e6; /* Background color */
				display: flex;
				justify-content: center; /* Center horizontally */
				align-items: center; /* Center vertically */
				height: 100vh; /* Full height of viewport */
				margin: 0; /* Remove default margin */
			}

			.container {
				max-width: 400px;
				padding: 20px;
				background-color: #1b5633; /* Container background color */
				border-radius: 10px; /* Rounded corners */
				box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Shadow effect */
				text-align: center; /* Center align content */
				color: #e9e3e6; /* Set text color */
			}

			h2 {
				font-size: 24px;
				margin-bottom: 20px;
			}

			.form-control {
				margin-bottom: 20px;
				border-color: #9a8f97; /* Input border color */
			}

			.btn-primary {
				background-color: #ffc629; /* Button background color */
				color: #1b5633;
				border-color: #ffc629; /* Button border color */
			}

			.btn-primary:hover {
				background-color: #736f72; /* Button background color on hover */
				border-color: #736f72; /* Button border color on hover */
			}

			.alert-danger {
				margin-bottom: 20px;
				color: #ff0000; /* Error message color */
			}
		</style>
	</head>
	<body>
		<div class="container">
			<form class="form-signin" method="post" action="/login">
				<h2 class="form-signin-heading">Login</h2>
				<% if (error) { %>
				<div class="alert alert-danger" role="alert"><%= error %></div>
				<% } %>
				<div class="mb-3">
					<label for="inputUsername" class="visually-hidden">Username</label>
					<input
						type="text"
						id="inputUsername"
						class="form-control"
						name="username"
						placeholder="Username"
						required
						autofocus
					/>
				</div>
				<div class="mb-3">
					<label for="inputPassword" class="visually-hidden">Password</label>
					<input
						type="password"
						id="inputPassword"
						class="form-control"
						name="password"
						placeholder="Password"
						required
					/>
				</div>
				<button class="btn btn-lg btn-primary" type="submit">Login</button>
			</form>
		</div>
		<!-- Include Bootstrap's JavaScript files (optional) -->
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
	</body>
</html>
