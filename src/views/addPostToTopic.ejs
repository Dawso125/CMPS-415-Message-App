<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Add Post to Topic</title>
		<!-- Link to Bootstrap CSS file -->
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
			rel="stylesheet"
		/>
		<style>
			/* Custom styles for the page */
			body {
				font-family: Arial, sans-serif;
				background-color: #e9e3e6; /* Background color */
				display: flex;
				justify-content: center; /* Center horizontally */
				align-items: center; /* Center vertically */
				height: 100vh; /* Full height of viewport */
				margin: 0; /* Remove default margin */
			}

			.container {
				max-width: 600px;
				padding: 20px;
				background-color: #1b5633; /* Container background color */
				border-radius: 10px; /* Rounded corners */
				box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Shadow effect */
				text-align: center; /* Center align content */
				color: #e9e3e6; /* Set text color */
			}

			.home-heading {
				font-size: 36px;
				margin-bottom: 20px;
			}

			.form-label {
				color: #e9e3e6; /* Label color */
			}

			.form-control {
				border-color: #9a8f97; /* Input border color */
			}

			.btn {
				margin-top: 10px;
				padding: 10px 20px;
				background-color: #ffc629;
				color: #1b5633;
				text-decoration: none;
				border-radius: 5px;
				transition: background-color 0.3s;
			}

			.btn:hover {
				background-color: #736f72;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<h1 class="home-heading">Add Post to Topic</h1>
			<form action="/postToTopic" method="post" class="form-signin">
				<input type="hidden" name="title" value="<%= title %>" />
				<div class="form-group">
					<label for="post" class="form-label">Enter Post:</label>
					<input
						type="text"
						id="post"
						name="post"
						class="form-control"
						required
					/>
				</div>
				<button type="submit" class="btn">Submit</button>
			</form>
		</div>
	</body>
</html>
