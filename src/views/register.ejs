<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>The Roaring - Messenger App</title>
		<!-- Link to Bootstrap CSS file -->
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
			rel="stylesheet"
		/>
		<style>
			/* Custom styles for the page */
			body {
				font-family: Arial, sans-serif;
				background-color: #e9e3e6; /* Background color */
				display: flex;
				justify-content: center; /* Center horizontally */
				align-items: center; /* Center vertically */
				height: 100vh; /* Full height of viewport */
				margin: 0; /* Remove default margin */
			}

			.container {
				max-width: 600px;
				padding: 20px;
				background-color: #1b5633; /* Container background color */
				border-radius: 10px; /* Rounded corners */
				box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Shadow effect */
				text-align: center; /* Center align content */
				color: #e9e3e6; /* Set text color */
			}

			h2 {
				font-size: 24px;
				margin-bottom: 20px;
			}

			.form-label {
				color: #e9e3e6; /* Label color */
			}

			.form-control {
				border-color: #9a8f97; /* Input border color */
			}

			.btn {
				margin-top: 10px;
				padding: 10px 20px;
				background-color: #ffc629;
				color: #1b5633;
				text-decoration: none;
				border-radius: 5px;
				transition: background-color 0.3s;
			}

			.btn:hover {
				background-color: #736f72;
			}

			.text-danger {
				color: #ff0000; /* Error message color */
			}
		</style>
	</head>
	<body>
		<!-- Apply home-body class to the body -->
		<div class="container">
			<!-- Apply Bootstrap's container class -->
			<h2>Register: The Roaring</h2>
			<!-- Use heading level 2 -->
			<% if (error) { %>
			<p class="text-danger"><%= error %></p>
			<!-- Use Bootstrap's text-danger class for error message -->
			<% } %>
			<form method="post" action="/register">
				<!-- Use action="/register" for the form -->
				<div class="mb-3">
					<!-- Apply Bootstrap's mb-3 class -->
					<label for="username" class="form-label">Username:</label>
					<!-- Use Bootstrap's form-label class -->
					<input
						type="text"
						class="form-control"
						id="username"
						name="username"
						required
					/>
					<!-- Use Bootstrap's form-control class -->
				</div>
				<div class="mb-3">
					<!-- Apply Bootstrap's mb-3 class -->
					<label for="password" class="form-label">Password:</label>
					<!-- Use Bootstrap's form-label class -->
					<input
						type="password"
						class="form-control"
						id="password"
						name="password"
						required
					/>
					<!-- Use Bootstrap's form-control class -->
				</div>
				<button type="submit" class="btn btn-primary">Register</button>
				<!-- Use Bootstrap's btn and btn-primary classes -->
			</form>
		</div>
	</body>
</html>
